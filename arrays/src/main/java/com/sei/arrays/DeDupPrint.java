package com.sei.arrays;

import java.util.Arrays;

public class DeDupPrint {
	
    public void printUniqueArray(int[] unique) {
        System.out.println(Arrays.toString(unique));
    }

    public void printUniqueArray(Integer[] unique) {
        System.out.println(Arrays.toString(unique));
    }
}
